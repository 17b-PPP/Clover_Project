สร้าง Group เป็น Admin
ทำการออกแบบหน้าตรวจสอบสิทธิ์ผู้ใช้งานและประวัติการใช้งาน

ข้อกำหนดด้านฟังก์ชันการทำงานของหน้าระบบตรวจสอบสิทธิ์ผู้ใช้งาน:
-แสดงข้อมูล บทบาท , ชื่อ-นามสกุล, ชื่อผู้ใช้งาน , อีเมล
-มีฟังก์ชั่นดูหรือแก้ไขข้อมูล
-มีฟังก์ชั่นเพิ่มข้อมูลผู้ใช้งานใหม่ โดย ชื่อ-นามสกุล, เบอร์โทร, อีเมล , ชื่อผู้ใช้งาน และการกำหนดบทบาทสำหรับการใช้งานเว็บไซต์ซึ้งแบ่งเป็น Staff และ Admin 
-มีฟังก์ชั่นระงับผู้ใช้งาน

ข้อกำหนดด้านฟังก์ชันการทำงานของหน้าระบบตรวจสอบสิทธิ์ผู้ใช้งาน:
-แสดงข้อมูล เวลา , ชื่อผู้ใช้งาน , บทบาท , Action , รายละเอียด
โดยหน้านี้จะเป้นการเก็บข้อมูลเพื่อดูว่าผู้ใช้งานมีแต่ละคนมี Action อะไรบ้างระหว่างการใช้งาน ตัวอย่างเช่น การเข้าสู้ระบบ การเพิ่มสมาชิก การระงับสมาชิก เป็นต้น

ข้อกำหนดด้านการออกแบบ:
-เว็บไซต์สะอาดตาและทันสมัย
-การจัดวางตัวอักษรและระยะห่างที่ชัดเจน
-ไม่ใช่การไล่ระดับสี
-แสดงผลได้อย่างเหมาะสมบนเดสก์ท็อป

ข้อกำหนดด้านเทคนิค:
-ใช้คอมโพเนนต์ React ที่นำกลับมาใช้ใหม่ได้
-ใช้ประเภท TypeScript
-ห้ามใช้ฐานข้อมูลหรือการตรวจสอบสิทธิ์ภายนอก
-โครงกรต้องทำงานได้ด้วยคำสั่ง 'npm run dev'
-ตรวจสอบให้แน่ใจว่าคำสั่ง'npm run build'ทำงานได้สำเร็จ

ขั้นแรก สร้างแผนการดำเดินงานสั้นๆ

จากนั้นกำเดินการโครงการทั้งหมด
สุดท้าย รัน lint และ build และแก้ไขข้อผิดพลาดใดๆ ที่พบ